---
description: Build a feature or fix a bug in the Cash-Track monorepo using the orchestrated developer, reviewer, and tester agents
argument-hint: <what to build>
---

Implement the following in Cash-Track, carried through to a reviewed, tested, committed change:

$ARGUMENTS

Invoke the `cash-track:agentic-dev` skill and follow it exactly. If the request above is too vague to
turn into a requirements brief, ask clarifying questions before dispatching any agent.
